{
  "name": "aohd-team-splitter",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t [ bubleify --jsx h ] src/app.js -o bundle.js",
    "watch": "watchify -t [ bubleify --jsx h ] src/app.js -o bundle.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hyperapp": "^0.7.1"
  },
  "devDependencies": {
    "browserify": "^14.1.0",
    "bubleify": "^0.7.0",
    "watchify": "^3.9.0"
  }
}
